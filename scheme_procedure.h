#ifndef _SCEME_PROCEDURE_
#define _SCEME_PROCEDURE_

#include "cell.h"

cell proc_add(const cells & c); // +
cell proc_sub(const cells & c); // -
cell proc_mul(const cells & c); // *
cell proc_div(const cells & c); // /

#endif